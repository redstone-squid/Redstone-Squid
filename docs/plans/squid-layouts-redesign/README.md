# squid-layouts redesign series

Design-debt fixes for `packages/squid-layouts`, agreed 2026-08-21 while the framework is
still on the `local-development` feature branch. Each numbered file is one independently
landable unit of work with its own verification section; this file fixes the order and
the reasons for it.

The audit behind these plans covered the whole package (~8k lines), the nine production
consumers, and a comparison against CascadeUI
(https://github.com/HollowTheSilver/CascadeUI). Everything found is captured: plans
01–07 are the dependency-ordered core, 08–17 are agreed improvements whose order is
flexible, and `90-deferred.md` records what was consciously rejected or postponed so it
is not re-derived later.

## Status

Completed plans are archived under `docs/plans/completed/squid-layouts-redesign`.

The active index now intentionally remains only for:

- `32-demand-driven.md`
- `../completed/squid-layouts-redesign/36-classic-discord-target.md`
- `../completed/squid-layouts-redesign/53-view-adoption.md`
- `../completed/squid-layouts-redesign/58-public-api-narrowing.md`
- `../completed/squid-layouts-redesign/59-shared-pool.md`
- `../completed/squid-layouts-redesign/60-session-membership.md`
- `../completed/squid-layouts-redesign/61-role-panel.md`
- `../completed/squid-layouts-redesign/62-layout-view-adoption.md`
- `../completed/squid-layouts-redesign/63-stores-package.md`
- `../completed/squid-layouts-redesign/64-challenged-admission.md`
- `../completed/squid-layouts-redesign/66-ownership-boundaries.md`
- `../completed/squid-layouts-redesign/67-lifetime-naming.md`
- `../completed/squid-layouts-redesign/69-post-plan68-review.md`
- `../completed/squid-layouts-redesign/70-discord-py-interop.md`
- `71-target-product.md`
- `90-deferred.md`
- `91-prior-art.md`

## Core sequence (dependency-ordered)

| # | Plan | Why this position |
|---|------|-------------------|
| 01 | [Mount delivery atomicity](01-mount-atomicity.md) | Correctness bug (failed Discord edit bricks EXCLUSIVE controls), small, no design dependencies. First. |
| 02 | [Typed native event access](02-native-event-access.md) | Deletes an anti-pattern already copied into six files; unblocks migrating remaining old views without spreading it. |
| 03 | [Factory layer for composition](03-factory-layer.md) | Small, additive. Must land before 04: 04's migration snippets are factory-style and rely on `None`-filtering that does not exist today. |
| 04 | [Semantic parity, retire presets](04-semantic-parity-presets.md) | Closes the capability gaps (`Ladder`, accent, footer, lead media) keeping nine files on `primitives.presets`, migrates them, deletes the module. With 03, attacks the 15:1 semantic-bypass ratio from both ends. |
| 05 | [Variant ladders](05-variant-ladders.md) | `Fold` and `Choice` merge into one `Variants` node; self-contained solver change that simplifies the collapse loop before 06 reworks the same code. |
| 06 | [Pagination](06-pagination.md) | One cursor ladder under three slicers; the page index becomes a projection, so planning owns reconciliation and the mount draws once. Retires 01's cursor-snapshot workaround. |
| 07 | [`EditHandle`](07-ephemeral-lifetime.md) | The mount holds a way to write to its message, not a message. Closes the `notice()`-clobbers-the-panel bug and makes ephemeral refresh renew on every click; any time after 01. |

## Second tier (order flexible)

| # | Plan | Summary |
|---|------|---------|
| 08 | [Transactional state coverage](08-transactional-state-coverage.md) | Undeclared writes are reported rather than half-tracked: PARALLEL_READ rejects them, `sl.strict_state()` raises. `sl.state()` gains a no-initial form, `copy="ref"`, and typed overloads; the panels declare their view state. Shipped. |
| 09 | [Async data loading](09-async-data-loading.md) | `on_load` hook replaces every hand-rolled `load()`-before-mount, awaited inside 15's send seam. Expansion stops at a component that still owes a load, so `render()` never runs before `on_load`. Also retired the three superseded `ExpiringLayoutView` panels, fixing a live defect where the settings modals redrew through the dead compat layout. Its deferred `sl.resource` descriptor is resolved by 33. Shipped. |
| 10 | [Selection ownership](10-selection-ownership.md) | One rule for selected/opened/open: every stateful node takes `sl.controlled(value, on_change)` or `sl.managed(initial)`. Ownership is a value, not an inference from which fields were passed; the `None`-sentinel version is recorded as rejected. Shipped. |
| 11 | [Small warts sweep](11-small-warts.md) | Vestigial `Composition.interventions`, `Choices(minimum=1)` audit, custom-id collision test, post-01 `build_view` naming. Shipped. |
| 12 | [Session policy wrapper](12-session-policy.md) | Host-side `MountRegistry` keyed by `SessionKey`, with REPLACE/REJECT instance policies and `parent=` cascade; `Mount.on_finish`/`finished` and a set-valued `lock_to` are the framework side. Coexist dropped — an optional key already expresses it. After 15, whose `Destination` seam makes replace-only-on-successful-delivery enforceable. Shipped. |
| 13 | [Runtime devtools](13-devtools.md) | Owner-only `!dev ui` cog over `sl.discord.mounts()`, a weak registry of live mounts: list, inspect, scene dump. `Mount.snapshot()` is the diagnostics contract; `Mount.address` and a retained `plan` are what make it complete. Development mode only. Shipped. |
| 14 | [Routed actions](14-routed-actions.md) | First-class stateless controls (`Route` + `RoutedAction` + `Router`): replaces the five hand-rolled `DynamicItem` classes and the `RawItem`/cast splices, makes routed scenes serializable, and unblocks semantic authoring of vote/starboard/consent cards. Routes keep their existing custom ids, so posted messages survive. Explicitly *not* a durability feature. Shipped; extended by 16. |
| 15 | [Send ownership](15-send-ownership.md) | `Mount.send(Destination)` runs stage→deliver→commit framework-side; `reply_to`/`respond_to` adapters own the discord.py kwargs, `ui.destination` keeps audience policy host-side in the existing `Visibility` vocabulary. `bind` is deleted — its four remaining callers were hand-rolled flushes. Amends 01 §6 ("bind is the commit point"); before 09. Shipped. |
| 16 | [Routed actions, part two](16-routed-actions-part-two.md) | Extends routed actions with exact converter-aware identity and aliases, the reserved `r:` namespace and gone responses, explicit stateless selects, portable `route_id`, private route-table introspection, stable feature groups, a class-based middleware onion, and deadline-safe acknowledgement. Shipped. |
| 17 | [Deferred text](17-deferred-text-i18n.md) | The framework translates: a `Message` carries its own msgid through the tree and is resolved at plan time by the mount's `Localization`. Retires `t(self.locale, _("…"))` (712 sites) in favour of `L(t"…")`, makes `Chrome` a locale-free constant so the twelve chrome-less `render_static` calls stop rendering English, routes interpolation through `md()`'s escaping, and lets `Mount.localize` retranslate a live panel. Needs a custom Babel extractor — the stock one drops `L(t"…")` silently. Capability plus a three-module pilot; the rest migrates opportunistically. |
| 18 | [Forms](18-forms.md) | `FormSpec` value core + `sl.Form` descriptor sugar; modal submission finally dispatches `SubmitEvent` through the mount funnel; framework-owned validation retry loop (modal-after-modal-submit is API-forbidden, verified). Executes plan 02's reserved `ModalSpec`-promotion move; retires the seven `ErrorHandledModal` subclasses. Shipped. |
| 19 | [Patterns on forms](19-patterns.md) | The two-shell rule (pure `state → tree` core; component and router shells with control construction injected), `Wizard` with computed steps and orphan retention, `MultiChoicePanel` as the first consumer of Form's commit model — resolving 90's cross-page multi-select rejection. Shipped. |
| 20 | [Solver pressure](20-solver-pressure.md) | Glue budgets (`min`/`prefer`/`stretch`), break annotations (`unbreakable`, `keep_with_next`), balanced breaking, region pagination, `sl.paged` sugar. Agreed policy: implementation may be ugly, contracts may not; one global solver redesign once the warts are visible. Shipped. |
| 21 | [Cursor sources](21-cursor-sources.md) | Position-native cursor state and policy; `WindowSource` with validated capabilities and source-resolved windows; immutable async loading, shared navigation context, and capability-gated chrome. Plan 33 moved component-side fetching into a visible resource. Shipped. |
| 22 | [Global fit](22-global-fit.md) | Strategy selection becomes a bounded global search: adapters nominate candidate axes, assignments are ranked by the existing `CostVector`, and one is accepted only when the whole document actually solves. Makes the lossless-when-possible invariant true; the charter for 20 §8's redesign pass, so it lands before or with 20. From the 2026-08-21 external audit, verified in-repo. Shipped. |
| 23 | [Delivery receipts](23-delivery-receipts.md) | `Destination` returns the authority it actually created instead of the mount inferring permanence from ephemerality — a public interaction response is token-bound (`InteractionMessage.edit` → `edit_original_response`, verified on discord.py 2.7.1), and today's `permanent=True` blocks renewal while background refresh dies at 15 minutes. Also mints a free `@original` handle for `wait=False`. Amends 15 §1. Shipped. |

## Productization round (2026-08-22, order flexible; 24 before 25)

The explicit product decision 90's PyPI entry reserved: build for the library user
rather than waiting on bot consumers. Everything lands in `sl.discord` or portable
modules; the layout core stays presentational.

| # | Plan | Summary |
|---|------|---------|
| 24 | [Session registry move](24-session-registry-move.md) | Plan 12's `MountRegistry` moves into `sl.discord.sessions`, keyed on `Hashable` with `SessionKey` as the shipped convention. Supersedes the module's own host-side-placement rationale; `WhenOpen` stays two values. |
| 25 | [Devtools cog](25-devtools-cog.md) | Plan 13's cog becomes `sl.discord.devtools.DevTools(check=...)` over `sl.discord.mounts()` and a new public `routers(client)` accessor; gains `ui plan` and `ui metrics` from data `Mount.snapshot()` already carries. |
| 26 | [Topic bus](26-topic-bus.md) | Portable payload-free `TopicBus` (hashable topics, sync publish, a stated delivery contract, supervised drain, `drain()` test seam); `MountScheduler` gains the bus, `follow`, bounded fan-out and the expiry sweep, so a pre-expiry paused-status flush makes token death visible. Two publishing rules for hosts; the bot is the worked example. Shipped except for §E's live Discord experiment, which still needs a real message target. |
| 27 | [Snapshot stores](27-snapshot-stores.md) | `SQLiteSnapshotStore` (stdlib, zero deps) and `PostgresSnapshotStore` (asyncpg, optional extra) behind the untouched `LeaseSnapshotStore` boundary, plus the recovery reachability sweep (404 deletes the snapshot; unreachable is not gone). |
| 28 | [History](28-history.md) | `sl.history()` undo/redo: a new `on_action_commit` seam hands the recorder the action's whole state delta, and the author supplies external inverses per `record()`. Three tiers (framework-only, undoable, redoable); world first, then state; a failed inverse keeps the entry; an inverse may not write component state. In-memory v1; the consumer is `SettingsPanel`'s channel, locale and role-weight writes. |
| 29 | [Control vocabulary (Batch A)](29-control-vocabulary.md) | Toggle, portable modal multi-choice, upload values and visible downloads, Decision/confirm, CollectionEditor, and typed timestamps/time fields. Extends the semantic pipeline where typed scene data matters and uses pure patterns for lifecycle state. |
| 30 | [Structural patterns (Batch B)](30-structures.md) | A shared `CommitMode`, MultiChoicePanel immediate commit, `Editor` (sections of forms + preview + commit policy), and `Browser`/`Lookup` as resource-backed `WindowSource` components per 33. No new pagination abstraction — the survey validated the existing one. Shipped. |
| 31 | [Action ergonomics (Batch C)](31-action-ergonomics.md) | Guard admission vocabulary (cooldown/when/permission/once/rate_limit/until/all_of/any_of; `requires_role` is Discord-layer sugar) checked after 34's access stage and before the transaction, with chrome-routed private rejections; busy feedback as a watchdog-integrated scene patch; Wizard review + jump-back; `ScaleField` + `sl.rating`. Shipped framework-side; consumer migrations follow. |
| 32 | [Controlled ledgers, grids, and agreement](32-demand-driven.md) | `Roster`, `sl.tally`, mounted `Agreement`, `MATRIX`, exact `sl.discord.button_grid`, and the semantic `Grid` ladder. **Shipped.** |
| 33 | [Reactive async resources](33-resources.md) | `sl.resource(depends=(state_field,))` exposes synchronous `Pending | Ready | Failed` state, render-observed lazy loading, stale-result rejection and optimistic replacement. Visible and atomic delivery are two policies over one state machine; `SourceRankedList` is the first migration. Shipped. |
| 34 | [Safe session runtime](34-safe-session-runtime.md) | Required async mount access policies, typed session scopes and structured cardinality outcomes, then one durable runtime that owns fenced checkpoints, Discord reconnection, recovery reports, leases, expiry, and teardown. Intentionally breaking: `lock_to`, `MountRegistry`, and public `MountManager` are replaced rather than shimmed. |
| 35 | [Discord V2 fragments](35-discord-v2-fragments.md) | Measured sessionless Squid islands inside an existing host-owned `LayoutView`: full resource reservations, detached multi-item fragments, atomic preflight/attachment, assets, custom-ID auditing, and routed controls. Arbitrary live-view adoption remains rejected. Lands in three units (measure/audit, reservation axes, fragments); 36 needs only the first two. |
| 36 | [Classic Discord target](../completed/squid-layouts-redesign/36-classic-discord-target.md) | A real `discord.components-v1` target: named message-wide budget axes, classic limits, classic primitives chosen at capability-gated semantic lowering, an exact classic scene, a `discord.ui.View` renderer, and classic mounts over a shared `BaseView` mixin. Host contribution is a measured reservation over 38's presentation, not a fragment object. Depends on 38 and on 35 units 1–2. No in-tree consumer, so its evidence is synthetic tests plus discord.py cross-checks; the live gate is still open. Shipped. |
| 37 | [Runtime profiling](37-runtime-profiling.md) | Bounded immutable traces, counters, and histograms for dispatch, acknowledgement, rendering, delivery, MountScheduler, TopicBus, and routed actions; rebase is generation metadata rather than a terminal outcome, with owner-only inspection and raw export. Shipped; the optional OpenTelemetry bridge remains demand-driven. |
| 38 | [Discord presentations](38-discord-presentation.md) | `DiscordPresentation` — mode, content, embeds, view, and assets as one value — replaces the view-plus-files pair in `Destination`, `EditHandle`, and `Composition`, and turns `_legacy_fields` guessing into an explicit mode transition matrix with `DiscordModeError` on the irreversible V2 → classic edit. Breaking for host-written destinations. Independent of 36, which depends on it. Shipped; 36 is what now constructs a `CLASSIC` presentation. |
| 39 | [Ephemeral handoff](39-ephemeral-handoff.md) | An opt-in `RenewEphemeral` mount policy replaces an expiring private panel with a protected, non-mutating renewal screen; its click adopts fresh edit authority and restores the same mount on the same message. MountScheduler owns timing, while delivery continues to own visibility. |
| 40 | [Shared state](40-shared-state.md) | View state whose lifetime exceeds one mount and whose writes still belong to the action that made them: `sl.Shared[ScopeT]` namespace classes of `sl.cell()` descriptors, held and shared as ordinary objects, staged into the existing transaction, published as `(handle, descriptor)` addresses on 26's bus, and restored blindly by 28's history. A namespace holds the same `_Cell` a component does, so immutability, tracking, staging, rollback and deltas all come from the machinery `sl.state()` already uses ([41](41-reactivity-cells.md)). No store, no keyed lookup, no lifetime subsystem — the handle is the state and GC is the lifetime. Opened by fixing `transaction()`'s unrolled-back commit path. Naming a cell without reading it is `sl.addresses(lambda: handle.cell)`, which reuses the render collector rather than adding a typeless method to the handle. **Shipped.** |
| 41 | [Reactivity cells](41-reactivity-cells.md) | Replaces `sl.state()`'s mutation proxies with values that are replaced, never mutated — enforced by the type checker through overloads that declare a `dict`/`list`/`set` default as its read-only ABC — automatic read tracking in place of hand-maintained `depends=`, and pull-based computeds that recompute when a version they read has moved. No dependent lists: every reference points reader to source, so a per-message component is collected. Deletes `ReactiveList`/`Dict`/`Set`, `_observe`, `_plain`, `_Snapshot` and `CopyMode`; `copy="ref"` becomes `opaque=True`. Writes stage in the transaction's overlay, so read-your-writes survives an `await` without publishing a dirty read, and rollback is dropping the overlay. Rewrites 40 onto the same node, so a computed can depend on shared state. Undeclared transaction-time writes always raise and `strict_state()` is deleted — the lax mode was the only one that let the write land. Chosen from three prototypes in `spikes/41/`. **Shipped.** |
| 42 | [Redundant edits](42-redundant-edits.md) | Nothing in the delivery path compared a staged render against the one on screen, so a refresh scheduled by an address rather than by a difference edited Discord anyway — and every delivery rotates control ids, so an in-flight click could lose a race to a repaint that changed nothing. Compare at the scene fingerprint with asset and handler-key guards; suppressed renders commit runtime bindings and durable state without rotating the visible generation, and report `PresentationStatus.UNCHANGED` plus scheduler/mount counters so silence is distinguishable from a dead subscription. **Implemented.** |
| 43 | [Mount defaults](43-mount-defaults.md) | `MountDefaults` holds the host-wide half of `Mount.__init__`; `SessionRegistry` holds one and `open` accepts a component. The Cascade class-body policy surface is rejected and recorded in 90: access and scope need an actor, and one component opens under different policies. Retires the bot's `create_mount`. |
| 44 | [Migration guide](44-migration-guide.md) | `docs/migrating.md` for an existing discord.py bot, ordered by how little has to change: fragments, classic, routed controls, mounts, forms, a lifecycle mapping table. Docs only. |
| 45 | [Topic bridge](45-topic-bridge.md) | `TopicCodec` plus a host-owned `PostgresTopicBridge` over `LISTEN`/`NOTIFY` for encodable host topics — addresses, never state; `Shared` cells stay local. The render worker is the first second process. Not a relay on the bus, not a store. **Shipped.** |
| 46 | [Declarative record](46-declarative-record.md) | `sl.action(..., record=history)` records a tier-1 entry inside the action transaction; auto-recording stays rejected and 28's contract is unchanged. The binding carries the label the entry takes, so a row that collapses into a picker keeps recording. **Shipped.** |
| 47 | [Topic values](47-topic-values.md) | `type Topic = Hashable` becomes a frozen `Topic(kind, key)` value with `CellAddress` for shared-cell identities and `type Address = Topic | CellAddress` as what the bus carries — which makes encoding total, so `KindKeyCodec` ships as the bridge's default and 45's host codec demotes to an override. Phase 2 adds `sl.watch(*topics)`: a valueless versioned cell read inside a `sl.resource`, so the mount follows what its render actually read (41's thesis, applied to named topics) and 26 §B4's subscribe-before-read race and §B6's rehydration gap both close. `watch=` on `sl.resource` was designed and rejected — longer than the statement it sugars. Phase 3 deletes the bot's `follow_resource`. **Shipped.** |
| 48 | [Resource writes](48-resource-writes.md) | `Resource.replace` wrote `_state` directly, so a rolled-back action kept its replaced value and every other reader saw it before the commit -- the one write in the package that did not stage. It now joins the action through the existing `ActionParticipant` seam, with `Resource.state` reading the staged replacement back. No new primitive; `action_participant()` is just the read half of `join_action`. Unblocks 47 phase 3. **Shipped.** |
| 54 | [Resource chains](54-resource-chains.md) | A resource could not derive from another resource: reading one tracked nothing, so a dependent went silently stale, and the bot had to fuse `(build, node)` into one loader. `await self.other` inside a loader settles it, tracks it after the value is in hand, and orders the chain -- the frontend cannot discover it, because the pending set is what the *render* touched. Versions move on every transition and bump the epoch, so a computed can derive from a resource too. Cycle detection generalises to one task-local stack across computeds and resources, naming the whole ring rather than "X reads itself". Deletes 47 phase 2's `resources` parameter. **Shipped.** |
| 55 | [Shared derivations](55-shared-derivations.md) | What a *derived* value on an `sl.Shared` namespace is. `sl.computed` already worked and had no test: it carries no address, so a mount follows the cells behind it. `sl.resource` was silently accepted and then died on `_owner.invalidate()`. A resource can move without any source moving -- a reload, a `replace`, a topic publish -- so it gets an address of its own and publishes where a value lands; a mount follows both it and its inputs. Publishing *is* the namespace's invalidation. One load serves every mount holding the namespace. **Shipped.** |
| 56 | [One declaration](56-one-declaration.md) | `sl.cell()` and `sl.state()` collapse into one. Every difference between them was a property of the owner rather than the declaration -- whether the field has an address, whether a render-time write is refused, whether it can be persisted -- so the author was restating what the class statement already said. `Shared._state_binding` supplies the address, per instance, so a descriptor on a shared mixin answers correctly for each owner. Deletes `_SharedCell` and both "you meant the other one" errors, since the mistake stops being expressible. **Shipped.** |
| 57 | [Render writes](57-render-writes.md) | A render may not write the state it reads -- the rule `rendering()` already states, enforced for only half the states it names. Component state written mid-render draws a torn tree (`0/1` from one render) and swallows its own invalidation, so the tear is final; shared state raises. 56 sharpened it into one declaration with two invisible behaviours. Measured 0 occurrences across 1485 package tests and `tests/unit`, so it breaks nothing -- but a component *constructed* inside a render writes declared state legitimately, and needs the `note_born` exemption the transaction already has. `factory=`, `sl.computed`, `sl.resource` and `on_mount` replace every reason to do it. **Implemented.** |
| 63 | [Reactive extraction](63-reactive-extraction.md) | Extracts the reusable transactional kernel into zero-dependency `squid-reactive`: core cells/computed/transactions/participants, `Shared` plus OCC, tracked topics and `watch()`, a two-method synchronous bus protocol with a failure-isolating local implementation, exact committed-plus-one-staged subscription reconciliation, and optional resources. MountScheduler, run loops, PostgreSQL, durability, expiry, and profiling remain host opinions. **Implemented.** |
| 43 | [Candidate atomicity](43-candidate-atomicity.md) | External-review fixes at the mount's staged-candidate and lock-discipline seams: shared follows retire only on delivery, shared self-writes move the render-input revision, acknowledgement cannot wait behind a busy paint, presented hooks are synchronous under the render lock, and middleware's transaction boundary is explicit. **Shipped.** |
| 51 | [Screens](51-screens.md) | The per-open half of a session becomes a value, as 43 made the per-host half one: `Screen(name, scope, policy, access, options)` plus an `Opener` from which the access policy, `SessionKey` and `actor_id` are all derived once. `parent=` collapses the attach-or-open branch. The motivating defect is a load-bearing comment — the two consent sites keep one shared session key in sync by prose, and nothing fails a test when they drift. Class-body policy stays rejected; a screen is a value a host may build twice. |
| 52 | [Entity selects](52-entity-selects.md) | Discord's user/role/channel/mentionable pickers reach a message, not just a modal: an exact `EntitySelect` primitive plus a semantic `Entities` whose ladder degrades to author-enumerated `Choices` on a target without `actions.discord.entity` — the enumeration `settings_view.py` writes by hand today. Costs one component and one row with placeholder-only text, which is the number whose absence forced `SettingsPanel` down to one channel picker at a time. Restores 04-settings.md's five live pickers. |
| 53 | [View adoption](../completed/squid-layouts-redesign/53-view-adoption.md) | `adopt(view)` turns an **unsent** `discord.ui.View` into a `Component`: items translate to exact primitives, the legacy object keeps its state and callbacks, and Squid is the sole writer. An interaction proxy reads `edit_message(view=self)` as "flush" and refuses every second-writer call by name. Narrows 90's rejection rather than reversing it — a live view still owns a message and stays rejected, which `adopt()` enforces. Depends on 52. |
| 58 | [Public API narrowing](../completed/squid-layouts-redesign/58-public-api-narrowing.md) | `sl.__all__` grew to 372 names under a default-to-root policy; 100 have zero in-repo call sites. Replaces it with a three-tier surface (root authoring vocabulary, `sl.<domain>` namespaces, deeper escape hatches) and a promotion rule — a symbol is qualified until qualifying it is demonstrably noise — enforced by exact-set-equality contract tests instead of the old subset assertions. Eight mechanically-ordered commits: five pure renames/moves that defuse pre-existing landmines (shadowed `actions`/`entities` modules, a latent `topics.py` cycle, an undertested `ReactiveCycleError` export), a call-site codemod, the root narrowing itself, then the same treatment for `sl.discord`. |
| 59 | [Shared pools](../completed/squid-layouts-redesign/59-shared-pool.md) | A type-bound `SharedPool` owning one canonical `Shared` handle per scope, replacing the `setdefault` cache every host writes around every namespace — a cache that, as actually written, constructed and discarded a namespace on every hit. **Rewritten then shipped 2026-08-24.** The rewrite was forced by 63 having shipped `PersistedPool` without waiting for this, so the machinery already existed one package over: `SharedPool` lands in `squid_reactive` (`squid-stores` cannot import `squid_layouts`, so the original placement would have made the duplication permanent) and `PersistedPool` composes it across a `_create`/`_adopt` seam, which is what lets hydration finish before a handle becomes canonical. The scope half went the other way: the proposed `sl.discord.scopes` module was deleted rather than built, because `screens.py` already had `Opener`, a `Scope` enum and the guild check it was going to add — `Opener` gained one constructor per kind and `Screen.key` collapsed to one line, so a spelling was removed rather than a third added. The spike moved off `pool.at()` onto the inference the plan depended on and found two things: Pyrefly rejects `SharedT: Shared[ScopeT]` outright, which had been silently breaking the shipped `PersistedPool`, and the original signature inferred the wrong scope *silently*. Constructor overloads carry the relationship instead. Still no store: the CascadeUI finding it answers is the scoping, not the singleton. |
| 60 | [Session membership](../completed/squid-layouts-redesign/60-session-membership.md) | `SessionSnapshot.participants` held `{actor_id}` and nothing ever changed it, so `victim.participants - {actor_id}` was provably always empty and cross-user protection rested entirely on `attachment_actors`. Gives the field meaning: an explicit member set and a `capacity` integer mutated through `join`/`leave` under the lifecycle lock, with `participants` derived so a policy reads one field. Capacity is an `int` rather than a policy object, and rules beyond it are per-call synchronous predicates — candidate facts are async but set-independent and belong in the caller, set facts are pure and belong under the lock, and `expect=` covers the rule that is both. Durable joins checkpoint after releasing the lock as `_attach` does, which dissolves the deadlock an in-lock fence creates. Adds the dual of capacity — a per-member `quota` over a membership `domain`, enforced at opening as well as at joining because the opener joins by opening, under a per-user lock. Ships 34 §B.4's required lobby as `/layout lobby`, and closes 90's participant entry. **Shipped.** |
| 63 | [The stores package](../completed/squid-layouts-redesign/63-stores-package.md) | `discord/durability/` is already two packages wearing one name: `stores.py` (724 lines, stdlib only) and `postgres.py` (690, anyio plus what is now a `squid_reactive.topics` re-export) import nothing from `squid_layouts`, while the codecs, `ComponentRegistry` and `DurableSessionRuntime` beside them are all about `Mount`. The bot's only two durability imports are `PostgresTopicBridge`, in the portable half. Unit 1 moves that half to `packages/squid-stores` behind unchanged re-exports; unit 2 lands the CascadeUI persistence finding there as `ScopedStore` — a declared `Slot(name, codec, ttl, version)`, expiry enforced on read as well as by the sweep, Postgres deadlines computed from a duration in the database, TTL fixed from write rather than sliding — built *on* the extracted discipline instead of re-deriving it. The turnkey part is `PersistedPool`: hydrate in `on_load`, write back through `on_action_commit`, so a rolled-back action persists nothing while a failed persist never fails the action. Records the `discord/` split as the larger question it invites and does not answer. |
| 64 | [Challenged admission](../completed/squid-layouts-redesign/64-challenged-admission.md) | `@with_loading` already ships as 31 §2's `feedback=`, so the only real gap in the interaction-sugar finding is confirmation — and it is not a decorator problem. **Shipped 2026-08-24**, after being rewritten the day before. The motivating defect (`prompt_for_consent` awaited *inside* an action handler, so inside the transaction and, under EXCLUSIVE, inside the dispatch lock) was closed by inverting the prompt's `wait()` into a continuation, not by this plan: guard admission only reaches controls that accept `guard=`, and two of the three waits were behind `Toggle`. What shipped here is the library capability. `Guard.admit` gains a third outcome, `GuardDecision | Challenge`; a challenged press admits nothing, opens no transaction and releases the action lock, and the approval re-enters `dispatch` from the top so access lost or a cooldown started while the dialog was open still refuse it. `sl.guards.confirm` is the sugar, `ChallengeRunner` the queue that keeps the resumption out of the approving press's transaction (`start_soon` would not: a spawned task inherits the ContextVar). `AccountPanel._unlink` loses its armed flag, early return and relabelling. Three findings from building it: the routed key needed no new plumbing, the approval must be a counter rather than a flag or a two-question chain never converges, and `ClaimReviewComponent.reassign_armed` was never a consumer — it is armed by a service conflict, which a guard running before the handler cannot know about. |
| 65 | [Screen entry points](../completed/squid-layouts-redesign/65-screen-entrypoints.md) | `Screen.respond(sessions, component, interaction)` derives both delivery and opener identity for the interaction happy path. `SessionRegistry.open` narrows to constructed mounts, leaving component construction to `MountDefaults` and `Screen`; the unused component overload and `open_personal` are removed. |
| 66 | [Ownership boundaries](../completed/squid-layouts-redesign/66-ownership-boundaries.md) | An external review's ten "make ownership explicit" proposals, checked against the tree: most described decisions already shipped (`MountAddress`/`EditHandle` *is* its identity/authority split) or in flight (60). What its diagnostic question — who owns this, and what event ends it? — actually found was a confirmed bug: `PersistedPool` entered an anyio task group in `load()` and exited it in `close()`, which anyio refuses across tasks, and every test called both from one task. Shipped with three smaller ownership fixes: `ActionParticipant.prepare` now returns what `apply` takes (deleting an `assert` standing in for the contract), a candidate settles exactly once, and a session's mount graph is one dict instead of three containers. **Shipped 2026-08-24.** |
| 67 | [Lifetime naming](../completed/squid-layouts-redesign/67-lifetime-naming.md) | The pre-publication naming reset. A closed suffix taxonomy encoding lifetime in nouns was designed and rejected on measurement — 93 distinct suffixes, 60 used once — so lifetime is carried by six closed verbs and nouns owe one-meaning-per-word instead. Caught two public classes both named `MountSnapshot` with opposite lifetimes (a view of a live mount, and state that outlives the process), and `MemorySnapshotStore` whose every method already said `records`. Enforced by `tests/architecture/test_naming.py`. `Destination` and `Progress` remain genuinely collided and are the open decision. **Shipped 2026-08-24.** |
| 68 | [Action identity and isolation](../68-completion-audit.md) | Opened by a web-framework port review that stress-tested the model against concurrent actions, cancellation and browser history. Four of its eight proposals described in-tree defects; the audit that checked them is worth as much as the plan, because two of the four survived contact and two did not — a version check on history restores was reverted the same day for contradicting plan 28's deliberate blind restore, and refusing to install a resource result whose dependency moved turned out to be a regression against stale-while-revalidate, since `_recheck` already re-pends at the next read. What shipped in this directory is the isolation half: transactions are confined to the task that opened them, with `StaleReactiveContextError` for a child task that inherits one, and each resource load owns its tracked read set instead of writing into the resource's, which a superseded loader was polluting. The rest outgrew this series and lives at `docs/plans/68-*` — full strong-read OCC with `relaxed_read()`, conditional history over weak and strong targets, an immutable action ledger, causal DevTools, compensation through a transactional outbox, and a fake replicated SPI with two CRDT backend spikes. Isolation went round twice: chosen here as opt-in per action, shipped there as the default, then narrowed back on 2026-08-24 — a read-only read carries no precondition unless it is taken inside `strong_read()`, because a handler that reads a namespace for a permission check and writes something unrelated is the common shape and aborting it buys nothing. Version-over-equality lineage, which arrived with the strict default, was kept. **Shipped 2026-08-24**; see the completion audit for the phase-by-phase evidence and [the migration guide](../../plan68-migration.md) for the breaking cutover. |

| 69 | [Post-Plan 68 review fixes](../completed/squid-layouts-redesign/69-post-plan68-review.md) | Landed 2026-08-25. Seven correctness, lifecycle, documentation, quota, and concurrency findings from the Plan 68 review. Six were the same shape -- something was lost and nothing said so -- and each was fixed by preserving the missing information or making the loss loud, never by retaining more. The load-bearing one is replicated set removal: undo minted a fresh `add`, so undoing one replica's removal resurrected a value against a concurrent removal still in force; removals now carry identity and are reversed by name. Beside it: the local operation clock is restored from the log so a reused replica ID stops silently dropping the user's own new write, outbound overflow demands a resync instead of evicting deltas quietly, undo/redo/compensation restore the entry state when a fresh action is refused, the session quota counts against the membership an open leaves behind so replacing your own session stops being refused, and the challenge runner bounds active presses rather than only queued ones. The `strong_read()` example that claimed a read-only replicated read blocks an unrelated local write is corrected and now pinned by tests at both polarities. |
| 70 | [discord.py interop](../completed/squid-layouts-redesign/70-discord-py-interop.md) | Landed 2026-08-25. Four places where a host reaches around `sl.discord` to reach discord.py, and the `Context`/`Interaction` split that leaks into consumers. The load-bearing one is reachability: `MountDefaults` solved mount *construction* but is a value, and a value cannot be found from an `interaction`, so the bot minted a process-global `install_mount_defaults` for the 21 `create_mount` sites that hold no bot. `install(client)` returns a `LayoutHost` — the circular registry/runner/presenter assembly done once, keyed per client the way `routing._INSTALLED` already keys routers — and `LayoutHost.of(client | interaction | ctx)` is what the global is a bad substitute for. Takes the reachability half of the runtime facade [65](../completed/squid-layouts-redesign/65-screen-entrypoints.md) deferred, and leaves the policy half deferred. Three smaller gaps beside it: `edit_to(message)` as the missing fourth `Destination`, `send_to` overloads that accept a real `abc.Messageable` instead of requiring a cast, and `render_item` moved into the package so a host stops reaching into `presentation.layout`. |
| 71 | [Target product type](71-target-product.md) | `TargetProfile` is an eleven-field bag whose `limits` and `dialect` are both typed `object`-or-`None`, so the one fact planning turns on -- which Discord message shape is being compiled to -- is encoded five times (target id, `mode` marker, limits subclass, dialect singleton, `DiscordMode`) and reconciled by hand; `mount.py` recovers it by `isinstance`-ing the erased field. Four confirmed defects follow: `sl.paged()` under the classic target raises `AttributeError` because a `V2Limits`-annotated function reads `total_components` off a `ClassicLimits`; `actions.py` passes a `DiscordLimits` into a `V2Limits` parameter and type-checks; `Renderer[OutputT]` is declared and structurally unsatisfiable, since both Discord renderers narrow `SceneDocument` contravariantly; and seven `getattr(limits, ..., default)` reads invent caps the type erased. The fix makes the target a product of two axes -- a dialect owning the protocol (id, version, capabilities, mode, body type, limits type) and an `AdapterProfile` owning the library -- with everything else derived, `Target.triple` as the identity that lets two adapters for one protocol coexist, and limits split into `ComponentLimits` / `EmbedLimits` / mode-specific budgets so each function takes what it actually reads. The 2026-08-26 splits of `adaptation.py` and `measurement.py` moved every one of these sites and fixed none of them, which is why this is a type problem rather than a layout one. |
| 91 | [Prior art](91-prior-art.md) | An external survey (2026-08-24) placing the reactive model against Dioxus, Compose snapshots, Salsa, Clojure STM, Missionary, Incremental, Glimmer, Jotai and TanStack DB, checked line by line against the tree. Mostly confirmation — the staged overlay *is* a `MutableSnapshot`, `strong_read()` *is* `ensure`, `on_action_commit` *is* an agent dispatch, the epoch plus per-cell versions *is* Salsa's revision scheme, and a loader's reads already track through `await` as Dioxus's do. Four of its claims are wrong about this tree and each would be a regression if taken up, the largest being that Dioxus's "may be dropped at any `await`" should become the resource contract — it is available as an optional seam precisely so it need not be. What is genuinely absent is Salsa's durability tiers, and that names a latent cliff: `_EPOCH` is one global, so any write anywhere costs every settled node a full source walk. Filed with a measurement as its removal condition, not built. A second survey the same day, having read that answer, added three proposals neither round had listed: `commute` (Clojure's operation-semantics alternative to a three-way `merge=`, which is the right shape and is already what `squid-replicated` does — filed against a `ConflictDetail` sample from a contended workload, not against a design exercise), long-lived forks (the machinery exists; every consumer it names is already served by pattern-level staged/committed values, and it wants commute first), and retryable pure actions (rejected here — an action holds a single-use interaction token on a three-second clock, so there is nothing to retry into — and recorded as a porting note). It also rated `ensure` redundant on the assumption that every observed read is validated; that default was inverted on 2026-08-24, so `strong_read()` *is* `ensure` and is load-bearing. Not a plan — a reference, so the next survey is answered by pointing at a file. |
| 72 | [Incremental render caching](72-render-caching.md) | Make mounted work proportional to change: automatic dependency-aware component/subtree snapshots, exact and structural plan programs, certified local replanning under headroom, unchanged suppression before Discord allocation, and fresh-object renderer programs. Defaults remain bounded and per-runtime; explicitly shared caches retain only callback-free artifacts. |

## Relation to existing plans

`docs/plans/squid-layouts-migration.md` remains the tracker for deleting superseded
discord.py view classes. Its `primitives.presets` bullet is superseded by plan 04 here;
its "core design debts closed" list still describes the landed state this series builds
on.
