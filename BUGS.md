# BUGS

No known open bugs. The previous six entries (`/build submit` retry, the missing vote channel,
the edit-workspace change markers, `on_command_error`, the ephemeral edit 404, and the api
service's host port) were fixed; `git log BUGS.md` reaches the commits that closed them.
