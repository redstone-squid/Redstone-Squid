# BUGS

No known open bugs. The `admin records-lookup` unmatched-category report
(`b03322f1d85e`) was fixed as part of the systemic structured-error migration in
`docs/plans/structured-errors.md`; the application/domain architecture rule now rejects new bare
builtin raises.
